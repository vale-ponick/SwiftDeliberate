//
//  tasksReflect.swift
//  SwiftDeliberate
//
//  Created by Валерия Пономарева on 09.07.2026.
//

import Foundation

/*
📝 DevLog: Спринт по массивам (Задачи 3️⃣ и 4️⃣)
 
 Дата: 09.07.2026
 Проект: SwiftDeliberate
 
 🔥 Главные победы
 1.'Алгоритмический триптих' рулит! Расписала алгоритм на русском («Лист дубликатов») — и код лег без единой логической ошибки!
 2. Инженерное чутье: Сама глазами поймала косяк со сбросом переменной внутри цикла еще до запуска.
 3. Взяла высоту: Раскопала в своих архивах, адаптировала и сама написала reduce(into:) с кортежем массивов. Это чистый O(N) и уровень Мидла!⚠️
 
 4. Мои грабли:
 1. Тернарник ? :: Пыталась использовать его для вызова действий (.append()). Получила ошибку несовпадения типов () и [Int]. Запомнить:
        Тернарник только возвращает значения.
        Для действий — строго if-else.
 2. Замыкания: Забывала писать result. перед массивами внутри reduce. Они теперь живут внутри накопителя!
 
 💡 Главный психологический инсайт:
        - Моё огорчение, что я не могу написать всё с абсолютного нуля без подсказок — это ловушка ума.
        - Умение поднять свои старые архивы, найти нужный сниппет и адаптировать его под новую сложную задачу (как я сделала с кортежем) — это и есть настоящий навык коммерческого программиста.
        - Я не «поварёнок», я инженер, который учится комбинировать инструменты.
*/
/*
 1. Ошибаться с утра в синтаксисе скобок Set(...) — нормально. Главное — интуиция!
 
 2. Массивы для поиска (.contains) — это зло на больших данных (O(N²)).
    Если нужно проверять "а было ли такое число?", всегда нужно смотреть в сторону Set (O(1)).
 
 3. Декларативный Dictionary(grouping:by:) выглядит эффектно, но у него есть
    две проблемы: он жрёт память (копирует массивы) и полностью перемешивает порядок.
 
 4. Самый сбалансированный код объединяет функциональный стиль (reduce) и
    быстроту множеств (Set). Чтобы reduce не превращался в нечитаемую кашу,
    начальный кортеж нужно выносить в отдельную переменную (initialState).
 
 5. Generics (`<Element>`) и Расширения (`extension`) только звучат страшно.
    На деле это просто способ сделать один и тот же алгоритм ленивым и
    универсальным для любых типов данных.

 ### Задача 9️⃣: Сдвиг массива вправо

 - ✅ Решила двумя способами: срезы и цикл
 - 🧠 Поняла: `%` убирает лишние обороты
 - 📌 Срезы работают за O(1), не копируя память
 - ⚠️ Цикл работает за O(n * k) — медленнее, но понятнее
 - 💡 Вывод: срезы — для скорости, цикл — для понимания
 */

/* ### 🎬 Задачи на срезы (фильмы)

**Что сделала:**
- Топ-5 фильмов (prefix)
- Последние 3 фильма (suffix)
- Фильмы с 3-го по 7-й (диапазон)

**Где ошибалась:**
- ❌ Перепутала `suffix` — ожидала не те фильмы
- ❌ Забыла обернуть срез в `Array`
- ❌ В `middleMovies` использовала не тот диапазон (`1...5` вместо `2...6`)

**Что поняла:**
- `prefix` и `suffix` — работают с конца/начала
- `[n...m]` — требует точного знания индексов
- Срезы не копируют память, но `Array()` — копирует

**Вывод:**
- Срезы — мощный инструмент, но требующий внимания к индексам
- Лучше перепроверить границы, чем гадать на кофейной гуще */
/*
 📊 ПРОГРЕСС ПО ЗАДАЧАМ
 
 ✅ 1️⃣ Сумма
 ✅ 2️⃣ Максимум
 ✅ 3️⃣ Комплексная обработка
 ✅ 4️⃣ Дубликаты
 ✅ 5️⃣ Проверка сортировки
 ⬜ 6️⃣ Переворот массива
 ⬜ 7️⃣ Второй максимум
 ⬜ 8️⃣ Чётные → квадраты
 ⬜ 9️⃣ Сдвиг на K позиций
 ⬜ 🔟 Палиндром
 ⬜ 1️⃣1️⃣ Перемешать
 ⬜ 1️⃣2️⃣ Сжать дубликаты
 
 ### Задача 5️⃣: Проверка сортировки
 
 - ✅ Решила за O(n), без `sorted()`
 - ✅ Использовала `assert` для проверки
 - 🚕💨 Поняла: проверка одного прохода — это быстрее, чем создание копии
 - 📌 Запомнила: сортировка — O(n log n), проверка — O(n)
 */
