//
///  tasksReflect.swift
//  SwiftDeliberate
//
//  Created by Валерия Пономарева on 09.07.2026.
//

import Foundation

/*
 📐🚕💨 DevLog: Спринт по коллекциям (Массивы + Словари)

 📅 Период: 09–17.07.2026

 🧠 ГЛАВНЫЕ ПОБЕДЫ:

 1. 26 задач — полностью закрыты
 2. 3 файла с задачами — структурированы
 3. Шпаргалка по словарям — создана
 4. reduce(into:) — освоен на уровне Middle
 5. Fisher‑Yates shuffle — реализован вручную
 6. Срезы (prefix, suffix, диапазоны) — закреплены на практике

 ⚠️ ГРАБЛИ (общие для всех коллекций):

 1. Тернарник не для действий (`.append()`), только для возврата значений.
    ✅ Решение: использовать if-else для действий.

 2. Забывала `result.` в `reduce(into:)`.
    ✅ Решение: всегда указывать результат.

 3. `reversed()` возвращает коллекцию, а не массив → нужен `Array()`.
    ✅ Решение: `Array(array.reversed())`.

 4. `assert` для `compress` проверяла `!=`, хотя массив мог не измениться.
    ✅ Решение: проверять `.count <=`.

 🔥 ИНСАЙТЫ (общие):

 1. Set — O(1) для поиска, Array — O(n).
 2. Срезы не копируют память, но удерживают ссылку на оригинал.
 3. Fisher‑Yates — простой и эффективный алгоритм перемешивания O(n).

 📌 ⚜️ THE GOLDEN RULES (общие):

 ⚜️ "Structure is the foundation of maintainable code."
    → Структура — это фундамент поддерживаемого кода.

 ⚜️ "Reflection is a tool for growth."
    → Рефлексия — это инструмент роста.

 ⚜️ "Use Set for uniqueness, Array for order."
    → Для уникальности используй Set, для порядка — Array.

 ⚜️ "filter and map never mutate the original array."
    → filter и map никогда не изменяют исходный массив.

 ✅ ПРОГРЕСС:

 Массивы (21 задача):
 1️⃣-9️⃣  Базовые задачи (сумма, максимум, дубликаты, сдвиг и др.)     ✅
 🔟-1️⃣2️⃣ Второе по величине, парсинг, сумма уникальных             ✅
 1️⃣-9️⃣  Срезы (топ-5, палиндром, compress, частоты)               ✅

 Словари (5 задач):
 1️⃣ Добавление + средний балл   ✅
 2️⃣ Подсчёт вхождений           ✅
 3️⃣ Создание словаря из массивов ✅
 4️⃣ Группировка Person          ✅
 5️⃣ Группировка оценок          ✅

 🎯 СЛЕДУЮЩИЙ ШАГ:
 Optionals — теория, код, рефлексия.
 */
