//
//  bilboBagginsTreasure.md
//  SwiftDeliberate
//
//  Created by Валерия Пономарева on 20.07.2026.
//

📜 ТЗ: 📦 СУНДУК БИЛЬБО БЭГГИНСА (Easy + get/set)

/**
 Бильбо Бэггинс вернулся в Шир после похода к Одинокой горе и сложил свои самые ценные сокровища в сундук.
 Хоббит хочет быстро проверять, что именно лежит под каждым номером (get), а также без труда заменять вещи, если решит переложить их (set). Напр., если он захочет спрятать кинжал и положить вместо него свой любимый шёлковый жилет, а вместо сверкающей кольчуги — неприметный старый походный плащ, чтобы не привлекать внимания назойливых соседей Саквиль-Бэггинсов.
 Бильбо умеет считать, поэтому пользуется обычными числовыми индексами.
 
 🏗️ ТЗ:
 1. Создай структуру BilboBagginsChest.
 2. Внутри объяви изменяемое свойство items: [String] (массив строк), в котором изначально лежат 4 стартовые вещи:[0] "💎 Arkenstone"[1] "🪙 Gold of Erebor"[2] "🗡️ Sting"[3] "🛡️ Mithril Chainmail"
 3. Реализуй сабскрипт с одним параметром index: Int, содержащий оба блока — get и set:get — возвращает сокровище по индексу в виде строки.set — заменяет существующее сокровище по индексу новым значением (newValue).
 4. Заметь: операция не добавляет новые элементы в массив (не делает append), а строго обновляет старые по указанному номеру.
 5. 🛡️ УСЛОВИЯ БЕЗОПАСНОСТИ (Защита от краша)
 📊 ТАБЛИЦА БЕЗОПАСНОСТИ (отформатированная)
 Операция    Условие                                 Что делать / Что возвращать
 get    Индекс существует (внутри диапазона)         Возвращает сокровище (например, "💎 Arkenstone")
 get    Индекс вне диапазона (< 0 или ≥ длины)       Возвращает "📦 Such chest does not exist"
 set    Индекс существует (внутри диапазона)         Заменяет элемент: items[index] = newValue
 set    Индекс вне диапазона                         Ничего не делает (return), код не падает

 
 6. ЧТО БИЛЬБО ПЛАНИРУЕТ КЛАСТЬ В СУНДУК (set)
    В процессе кастомизации сундука Бильбо может использовать следующие вещи:
 "🧥 Silk vest with gold buttons"
 🧥 Old travel cloak" (старый походный плащ, подарок Торина)
 7. Он планирует заменить Жало (индекс 2) на жилет, а Мифриловую кольчугу (индекс 3) — на плащ. */
 
 // 8. 🧪 ТЕСТЫ ДЛЯ ПРОВЕРКИ (Обязательные)
 
 var chest = BilboBagginsChest()

// 1. Проверяем чтение (get)
print(chest[0])  // Должно вывести: "💎 Arkenstone"
print(chest[2])  // Должно вывести: "🗡️ Sting"
print(chest[4])  // Должно вывести: "📦 Such chest does not exist"
print(chest[-1]) // Должно вывести: "📦 Such chest does not exist"

// 2. Проверяем успешную замену (set)
chest[2] = "🧥 Silk vest with gold buttons"
print(chest[2])  // Должно вывести: "🧥 Silk vest with gold buttons"

chest[3] = "🧥 Old travel cloak"
print(chest[3])  // Должно вывести: "🧥 Old travel cloak"

// 3. Проверяем защиту от выхода за границы в блоке set
chest[5] = "💰 Secret gold" // Код НЕ должен упасть! Операция игнорируется.
print(chest[5])  // Должно вывести: "📦 Such chest does not exist"

// 🏆 ЗОЛОТОЕ ПРАВИЛО ХОББИТА

/** «Мудрый хоббит сначала проверит, существует ли в его норе нужная полка, а потом уже положит туда жилет. Так и ты в коде: и в блоке get, и в блоке set сначала строго проверяй индекс на валидность через guard, а только потом работай с элементами массива».*/

// 💬 Commit Message -> "feat: add BilboBagginsChest with safe subscript (get/set) and treasure replacement"

/* Задание выглядит невероятно фаново и профессионально одновременно. Переносите этот текст в свой задачник. Как договаривались — пишите код полностью сами и без подсказок! У вас всё получится! Напишите, когда будете готовы проверить результат или перейти к следующему шагу 🚀 Сундук Бильбо ждет! */
