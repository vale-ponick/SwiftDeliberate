//
//  subscriptTasksReflect.swift
//  SwiftDeliberate
//
//  Created by Валерия Пономарева on 18.07.2026.
//

import Foundation
/**
  📐🚕💨 DevLog: Сабскрипты
 
 🧠 СУТЬ САБСКРИПТА:
 Сабскрипт — это сокращённый вариант доступа к элементу
 структуры, класса, коллекции, списка или диапазона
 для получения + установки нового значения.

 Пример:
 someArray[index]      // получение
 someDictionary[key]   // получение
 someArray[index] = value  // установка

  📅 Период: [18.07.2026]

  🧠 ГЛАВНЫЕ ПОБЕДЫ:
  1. Поняла синтаксис subscript: доступ через []
  2. Научилась работать с двумерными массивами: seats[row][seat]
  3. Освоила проверку индексов через guard и if
  4. Узнала, как использовать заглушки в get и return в set

  ⚠️ ГРАБЛИ (ошибки, которые запомнила):
  1. [Ошибка 1]
     ✅ Решение: [как исправила]

  2. [Ошибка 2]
     ✅ Решение: [как исправила]

  🔥 ИНСАЙТЫ:
  1. Двумерный массив — как дом: сначала этаж (row), потом квартира (seat)
  2. Сабскрипт может принимать несколько параметров
  3. В продакшене лучше использовать словарь для поиска по имени

 🏆 GOLDEN RULES:
    1. Сабскрипт — это доступ через []
    2."Всегда проверяй индексы через guard или if" → защита от 'out of range'
    3. Сабскрипт может принимать несколько параметров.
    4. 'В get — возвращай заглушку, в set — return' → without crash!
    5. "Используй ?? для словарей" → безопасная работа с nil
    6. Двумерный массив = массив массивов: seats[row][seat].
    7. В продакшене клиенты приходят по имени, а не по индексу. Всегда добавляй слой маппинга (словарь) для удобства и безопасности.
 
 📝 КАК ЧИТАТЬ seats[row][seat]:
 
 seats[row][seat]
   │      │
   │      └─── номер квартиры на этаже
   └────────── номер этажа
 
 📝 RULE:
 seats[row][seat] → сначала этаж (row), потом квартира (seat)
 
 «Вложенный массив — это как многоэтажный дом. Сначала заходишь на этаж (row), потом открываешь дверь квартиры (seat).»
 
 ☕️🍩 Синтаксис Swift требует указать оба шага подряд: seats[row][seat]».
 
 ✅ ПРОГРЕСС:
  Сабскрипты — теория + практика ✅

  🎯 СЛЕДУЮЩИЙ ШАГ:
  controkFlowTasks
 */
