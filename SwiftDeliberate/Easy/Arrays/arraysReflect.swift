//
//  arraysReflect.swift
//  SwiftDeliberate
//
//  Created by Валерия Пономарева on 06.07.2026.
//

import Foundation

/* 🧠 Моя рефлексия: Ловушки при работе с массивами
 
 ❌ Ошибка 1: Конфликт именования и копипаст (Шаг 6)
 - Что сделала: Скопировала `.map` из предыдущего шага, назвала переменную `oddNums` (нечетные), а в ассерте проверяла четные.
 - Почему: Мозг замылился из-за противоположных понятий и автопилота после успешного шага.
 - Вывод: Сначала писать имя переменной, отражающее КОНЕЧНЫЙ результат (`evenNumsStrings`), а потом код. Конвейер проверять справа налево.
 
 ❌ Ошибка 2: Ловушка ArraySlice и удержание памяти (Шаг 11 + Тест)
 - Чего не знала: Что методы вроде `.prefix(3)` или `.suffix()` возвращают тип `ArraySlice`. Это не массив, а просто "окошко" в оригинальный массив.
 - В чем опасность: Этот срез держит ссылку на ВЕСЬ огромный исходный массив в памяти. Если срез живет долго — память не освобождается (утечка!).
 - Мой косяк в коде: Написала `Array(prices).prefix(3)` — это сделало копию всего массива ДО обрезки, и на выходе все равно остался срез.
 - Правильное решение: `let topCheapArray = Array(prices.prefix(3))` — инициализатор `Array()` должен стоять в самом конце конвейера, чтобы запечатать срез в новый, независимый от оригинала кусок памяти.
 
 💡 Главное открытие:
 Мои навыки прохождения тестов помогли выбить 10/12, логически вычисляя правильные ответы по их академическому тону. Но реальный срез знаний дает только пустой экран Xcode. Ошибаться в тестах и assert'ах — это круто, система проверок отработала штатно и подсветила баг до релиза.
 */
